@import "common";
#admin-menu {

    span, em, div, ul, li {

    }
    a {

    }
    .status {
          margin-left:40px;
          font: 12px 'PT Sans', sans-serif;
          color: white;
            a {
            font: bold 12px 'PT Sans', sans-serif;
            color: white;
            }
    }
    ul li {
      float:left;
      a {
      font: bold 12px 'PT Sans', sans-serif;
      color: white;
      margin-left:20px;
      }
    }
    ul { }
    li { }
}
