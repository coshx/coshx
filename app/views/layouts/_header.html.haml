#header-content.row

	.column
		= link_to image_tag("logo.png"), root_path

	%nav
		%ul#main-nav
			// = tab_to "HOME", root_path,
			= tab_to "Get to know us", about_path, active_tab("#{about_path}$")
			= tab_to "Our work", projects_path, active_tab("#{projects_path}$")
			= tab_to "Log book", blog_posts_path, active_tab("#{blog_posts_path}$")
			= tab_to "Careers", jobs_path, active_tab("#{jobs_path}$")
			= tab_to "Contact", contact_path, active_tab("#{contact_path}$")


//=render 'jobs/jobs'
